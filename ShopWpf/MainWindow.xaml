<Window x:Class="ShopWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShopWpf"
        mc:Ignorable="d"
        Title="Api Controller" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Developer">
                <Grid ShowGridLines="True" Background="#FFE5E5E5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    
                    <DataGrid Grid.Column="0" Grid.Row="0" d:ItemsSource="{d:SampleData ItemCount=5}"/>
                    <Button Grid.Column="0" Grid.Row="1" Height="10" Background="red"></Button>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
